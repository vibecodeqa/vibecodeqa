{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vibecodeqa.online/standards/references.schema.json",
  "title": "VibeCode QA External Standards References",
  "type": "object",
  "required": ["version", "lastReviewed", "purpose", "categories"],
  "properties": {
    "$schema": { "type": "string" },
    "version": { "type": "integer", "minimum": 1 },
    "lastReviewed": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" },
    "purpose": { "type": "string" },
    "policy": {
      "type": "object",
      "properties": {
        "sourcePreference": { "type": "array", "items": { "type": "string" } },
        "authoringRule": { "type": "string" },
        "linkHealth": {
          "type": "object",
          "required": ["status", "rule"],
          "properties": {
            "status": { "type": "string" },
            "rule": { "type": "string" },
            "versioningRule": { "type": "string" }
          },
          "additionalProperties": false
        },
        "appliesToRule": { "type": "string" }
      },
      "additionalProperties": false
    },
    "categories": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "title", "description", "references"],
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z0-9-]+$" },
          "title": { "type": "string" },
          "description": { "type": "string" },
          "references": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["id", "title", "publisher", "url", "appliesTo", "topics", "useFor"],
              "properties": {
                "id": { "type": "string", "pattern": "^[a-z0-9-]+$" },
                "title": { "type": "string" },
                "publisher": { "type": "string" },
                "url": { "type": "string", "format": "uri" },
                "expectedRedirectUrl": { "type": "string", "format": "uri" },
                "versionPolicy": { "type": "string" },
                "appliesTo": {
                  "type": "array",
                  "items": { "type": "string", "pattern": "^[a-z0-9-]+$" },
                  "uniqueItems": true
                },
                "topics": {
                  "type": "array",
                  "items": { "type": "string", "pattern": "^[a-z0-9-]+$" },
                  "uniqueItems": true
                },
                "useFor": { "type": "array", "items": { "type": "string" } }
              },
              "additionalProperties": false
            }
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
